@model SaniShop.Models.SalesDetailModel

@{
    ViewBag.Title = "SalesIndex";
}

<h2>Sales Home</h2>
@using (Html.BeginForm("Index", "Sales"))
{

    <html>

    <body>
        <div>
            <table>
                <tr>
                    <td>CustomerName</td>
                    <td>

                        @Html.TextBoxFor(m => m.Quantity, new { @class = "inputComn" })
                    </td>
                </tr>
                <tr>
                    <td>Iteam </td>
                    <td>
                        @Html.DropDownListFor(m => m.Product_id, Model.Productname, "--Select One--")
                        @*@Html.TextBoxFor(m => m.Product_id, new { @class = "inputComn" })*@
                    </td>
                </tr>
                <tr>
                    <td>Price </td>
                    <td>
                        @Html.TextBoxFor(m => m.Product_price, new { @class = "inputComn" })
                    </td>
                </tr>
                <tr>
                    <td>

                    </td>
                </tr>

                <tr>
                    <td colspan="2" style="text-align:center">
                        <input type="submit" name="save" value="save" />
                    </td>
                </tr>
             
            </table>
        </div>
        @*<div >
            <input  type="submit" class="contactBtn signupBtn" value="Submit" />
        </div>*@
    </body>
</html>
}